info:
  name: Get All Pipelines
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v1/pipeline/pipelines"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

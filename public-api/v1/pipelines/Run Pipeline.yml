info:
  name: Run Pipeline
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/pipeline/run"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "pipelineId": ""
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

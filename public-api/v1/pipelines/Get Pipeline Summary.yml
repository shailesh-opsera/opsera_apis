info:
  name: Get Pipeline Summary
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/pipeline/summary"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "pipelineId": ""
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

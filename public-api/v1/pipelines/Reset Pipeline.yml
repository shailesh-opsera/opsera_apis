info:
  name: Reset Pipeline
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/pipeline/reset"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "pipelineId": ""
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

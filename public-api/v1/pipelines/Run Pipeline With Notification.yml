info:
  name: Run Pipeline With Notification
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/pipeline/run/notification"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "pipelineId": "",
          "stepId": "",
          "notificationMessage": ""
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

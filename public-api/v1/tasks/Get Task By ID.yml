info:
  name: Get Task By ID
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/tasks/summary"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "taskId": ""
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

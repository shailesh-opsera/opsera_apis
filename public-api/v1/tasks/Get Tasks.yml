info:
  name: Get Tasks
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v1/tasks/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

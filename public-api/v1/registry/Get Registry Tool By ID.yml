info:
  name: Get Registry Tool By ID
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/registry/summary"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "toolId": ""
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Update Registry Tool
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/registry/update"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "toolId": "",
          "tool": {}
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Demo
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v1/registry/demo"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "toolId": ""
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

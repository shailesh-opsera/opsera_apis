info:
  name: Get Schema
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/scim/v2/Schemas/<id>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

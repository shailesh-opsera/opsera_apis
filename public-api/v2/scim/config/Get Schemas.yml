info:
  name: Get Schemas
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/scim/v2/Schemas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

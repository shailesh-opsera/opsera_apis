info:
  name: Service Provider Config
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/scim/v2/ServiceProviderConfig"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

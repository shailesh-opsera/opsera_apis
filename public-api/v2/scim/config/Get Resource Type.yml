info:
  name: Get Resource Type
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/scim/v2/ResourceTypes/<id>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

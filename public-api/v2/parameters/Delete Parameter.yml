info:
  name: Delete Parameter
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl_publicApi}}/api/v2/parameters/<parameterId>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

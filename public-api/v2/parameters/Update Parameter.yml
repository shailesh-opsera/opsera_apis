info:
  name: Update Parameter
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl_publicApi}}/api/v2/parameters/<parameterId>"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "DEPLOY_ENV",
          "value": "staging",
          "type": "string",
          "description": "Target deployment environment - updated to staging"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Parameter By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/parameters/<parameterId>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

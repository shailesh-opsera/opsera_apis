info:
  name: Pipeline Delete
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v2/actions/pipeline/delete"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "targetPipelines": [
            ""
          ]
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

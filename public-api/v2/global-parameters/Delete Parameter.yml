info:
  name: Delete Parameter
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl_publicApi}}/api/v2/global-parameters/<customParameterId>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

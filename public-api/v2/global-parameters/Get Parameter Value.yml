info:
  name: Get Parameter Value
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/global-parameters/<customParameterId>/value"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

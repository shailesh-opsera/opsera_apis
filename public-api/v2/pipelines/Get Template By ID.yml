info:
  name: Get Template By ID
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/pipelines/templates/<templateId>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

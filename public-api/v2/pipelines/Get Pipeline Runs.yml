info:
  name: Get Pipeline Runs
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/pipelines/<pipelineId>/runs"
  params:
    - name: limit
      value: 5
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

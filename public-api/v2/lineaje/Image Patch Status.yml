info:
  name: Image Patch Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/lineaje/image/patch/status/<uuid>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Mappings
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/data/mapping"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

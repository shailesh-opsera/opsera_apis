info:
  name: Get Registry
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/tools/registry"
  params:
    - name: tool_identifier
      value: github
      type: query
      disabled: true
    - name: fields
      value: _id,name,tool_identifier
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

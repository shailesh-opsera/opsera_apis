info:
  name: Get User by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/usermanagement/user/<userId>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

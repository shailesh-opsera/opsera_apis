info:
  name: Update User Site Role
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl_publicApi}}/api/v2/usermanagement/user/site-role"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "userId": "507f1f77bcf86cd799439011",
          "siteRole": "PowerUsers"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Group Memberships
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/usermanagement/group/<groupName>/memberships"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

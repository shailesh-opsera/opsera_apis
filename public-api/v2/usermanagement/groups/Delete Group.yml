info:
  name: Delete Group
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl_publicApi}}/api/v2/usermanagement/group/<groupName>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

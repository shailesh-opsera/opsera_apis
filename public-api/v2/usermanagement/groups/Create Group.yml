info:
  name: Create Group
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v2/usermanagement/group"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "groupName": "developers-team",
          "groupMemberUserIds": ["507f1f77bcf86cd799439011", "507f1f77bcf86cd799439012"]
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

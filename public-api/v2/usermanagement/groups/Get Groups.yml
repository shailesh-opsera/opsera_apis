info:
  name: Get Groups
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/usermanagement/groups"
  params:
    - name: search
      value: dev
      type: query
      disabled: true
    - name: id
      value: developers-team,qa-team
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

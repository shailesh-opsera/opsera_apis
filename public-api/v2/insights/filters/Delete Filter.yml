info:
  name: Delete Filter
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl_publicApi}}/api/v2/insights/filters/<filterId>"
  body:
    type: json
    data: |-
      {
          "mappingId": "<mappingId>"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

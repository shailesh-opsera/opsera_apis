info:
  name: Get Hierarchy Levels
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/insights/tool-mappings/hierarchy-levels"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Operation Status
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/insights/tool-mappings/operation/<operationId>/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

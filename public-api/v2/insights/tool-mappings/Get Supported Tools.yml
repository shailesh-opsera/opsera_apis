info:
  name: Get Supported Tools
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/insights/tool-mappings/supported-tools"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Dashboard
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/insights/dashboards/<id>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

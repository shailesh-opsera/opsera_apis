info:
  name: View Dashboard
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl_publicApi}}/api/v2/insights/kpis/audit-logs/dashboard/<dashboardId>/view"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Recently Viewed Dashboards
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl_publicApi}}/api/v2/insights/kpis/audit-logs/dashboard/recently-viewed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
